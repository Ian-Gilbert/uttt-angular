import { MarkType } from "../mark-type.enum";
import { MoveObject } from "../move-object.model";

export const moveObjectMock: MoveObject = {
    mark: MarkType.PLAYER1,
    lbIndex: 0,
    markIndex: 0
}